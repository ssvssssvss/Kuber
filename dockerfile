# Используем официальный Python-образ
FROM python:3.9-slim

# Копируем файл приложения в контейнер
COPY app.py /app.py

# Устанавливаем команду, которая будет запускаться при запуске контейнера
CMD ["python", "/app.py"]